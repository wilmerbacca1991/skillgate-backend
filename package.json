{
  "name": "skillgate-backend",
  "version": "1.0.0",
  "description": "Express + MongoDB + Socket.IO backend for SkillGate.",
  "main": "server.js",
  "scripts": {
    "test": "npm run test:grading; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run test:ai; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run test:attempt; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run test:feature-mvp; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run test:architecture-boundary",
    "test:grading": "node scripts/grading-smoke-test.js",
    "test:ai": "node scripts/ai-feedback-smoke-test.js",
    "test:attempt": "node scripts/attempt-summary-smoke-test.js",
    "test:feature-mvp": "node scripts/feature-mvp-smoke-test.js",
    "test:architecture-boundary": "node scripts/architecture-boundary-smoke-test.js",
    "seed:data": "node scripts/seed-demo-data.js",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "express-validator": "^7.3.2",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.6.3",
    "morgan": "^1.11.0",
    "multer": "^2.1.1",
    "nodemailer": "^8.0.11",
    "openai": "^6.42.0",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "socket.io-client": "^4.8.3"
  }
}
